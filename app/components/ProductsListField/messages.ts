import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.products-list-field';

export default defineMessages({
  productsListFieldLabel: {
    id: `${scope}.productsListFieldLabel`,
    defaultMessage: 'Products',
  },
});
